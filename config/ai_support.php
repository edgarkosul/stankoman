<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| ИИ-ассистент магазина
|--------------------------------------------------------------------------
|
| Чат и эмбеддинги идут через один шлюз — aitunnel.ru, OpenAI-совместимые
| маршруты /v1/chat/completions и /v1/embeddings. Российский сервис, оплата
| в рублях, трансграничной передачи персональных данных нет.
|
| Форма конфига и большинство чисел перенесены из kratonshop — там они
| выведены замерами, а не выбраны, и переигрывать их заново значит платить
| за один и тот же поиск дважды. Где число опиралось на СВОЙ каталог,
| оно пересчитано здесь и помечено датой — см. `product_description_limit`
| и белый список страниц.
|
| Всё, что берётся из env, обязано читаться через config() в коде: на проде
| конфиг кэшируется, и прямой getenv() там ведёт себя иначе.
|
*/

return [

    /*
    |--------------------------------------------------------------------------
    | Шлюз
    |--------------------------------------------------------------------------
    |
    | Один ключ на чат и на эмбеддинги. Авторизация — Bearer.
    |
    | ВАЖНО про персональные данные: маскирование на шлюзе — это настройка
    | САМОГО КЛЮЧА (Панель → Ключи), а не параметр запроса. Проверено
    | 10.09.2026 запросом GET /v1/aitunnel/key: pii.mode = mask,
    | types = null, то есть все типы. Переключить это могут снаружи кода,
    | поэтому команда ai:kb-doctor состояние читает и ругается, а не верит.
    |
    | Покрытие детекторов неполное — перебор делал донор. Шлюз надёжно берёт
    | email, ИНН, ОГРН, паспорт, СНИЛС, карту (по контрольной сумме) и ФИО
    | с отчеством, но НЕ видит:
    |
    |   - телефон слитной записью 89211234567 и без кода страны;
    |   - имя без отчества («Сергей Петров») — намеренно, ради меньшего
    |     числа ложных срабатываний;
    |   - адрес прописью («на улице Народной, дом 12»).
    |
    | Эти три дыры закрывает наш PiiRedactor — он пишется под них, а не
    | дублирует то, что шлюз делает лучше. И он же остаётся единственным
    | слоем на маршруте /embeddings: там маскирование не применяется
    | вовсе, поэтому редактор вызывается и перед embed().
    |
    | ⚠️ Ключ сейчас общий: он же обслуживает проект turbo, и он же стоит
    | на деве и на бою intertooler. Следствия помнить три. Расход магазина
    | в статистике шлюза не отделить от чужого. Зацикленный тул-луп на деве
    | тратит те же деньги, что бот на бою. Настройки ключа — включая
    | маскирование выше — общие, и правка ради нас поедет в turbo.
    | К публичному запуску нужен свой ключ.
    |
    */
    'gateway' => [
        'base_url' => rtrim((string) env('AI_GATEWAY_BASE_URL', 'https://api.aitunnel.ru/v1'), '/'),
        'key' => env('AI_GATEWAY_KEY'),
        'timeout' => max(1, (int) env('AI_GATEWAY_TIMEOUT', 120)),
        'connect_timeout' => max(1, (int) env('AI_GATEWAY_CONNECT_TIMEOUT', 10)),

        /*
         * Ретраить осмысленно только сетевые сбои и 5xx. Повтор удавшегося
         * вызова — двойная оплата и риск дубля ответа, поэтому мало.
         */
        'max_retries' => max(0, (int) env('AI_GATEWAY_MAX_RETRIES', 1)),
    ],

    /*
    |--------------------------------------------------------------------------
    | Модель диалога
    |--------------------------------------------------------------------------
    */
    'agent' => [
        /*
         * Аварийный оверрайд разработчика. Штатный тумблер для админа живёт
         * в настройках; здесь — рубильник на случай инцидента.
         */
        'enabled' => filter_var(env('AI_AGENT_ENABLED', true), FILTER_VALIDATE_BOOL),

        // Обе модели проверены на нашем ключе 10.09.2026 живым вызовом.
        'model' => env('AI_AGENT_MODEL', 'deepseek-v4-flash'),
        'max_tokens' => max(256, (int) env('AI_AGENT_MAX_TOKENS', 1024)),

        /*
         * Потолок шагов tool-use. У донора примерно каждый пятый прогон
         * упирался в этот предел и отдавал пустоту — исход штатный и должен
         * приводить к мягкой эскалации, а не к ошибке.
         */
        'max_iterations' => max(1, (int) env('AI_AGENT_MAX_ITERATIONS', 8)),

        /*
         * Кеш промпта. Нагрузка на ~96% состоит из ввода, поэтому это главный
         * рычаг по деньгам. DeepSeek кэширует префикс сам, но привязка
         * к провайдеру без session_id включается только ПОСЛЕ первого
         * попадания в кэш. С session_id — с первого запроса.
         *
         * Замер донора на deepseek-v4-flash, префикс 3061 токен:
         *   без session_id — cached=0 на всех попытках, 0.10 ₽ каждая;
         *   с session_id   — 1-я 0.06 ₽, далее cached=2816 и 0.02 ₽.
         * Втрое дешевле. Сессия остывает через 10 минут без запросов.
         *
         * В качестве session_id используем токен диалога: внутри одного
         * разговора системный промпт и правила магазина неизменны.
         */
        'session_affinity' => filter_var(env('AI_AGENT_SESSION_AFFINITY', true), FILTER_VALIDATE_BOOL),

        /*
         * Системный промпт собирается в коде из неизменяемого ядра и
         * редактируемого в админке слоя. Здесь не хранится намеренно:
         * ядро с гардами не должно быть переопределяемо из env.
         */

        /*
         * Потолок описания товара, уезжающего в модель.
         *
         * Число донора — 3000 — здесь не скопировано, а пересчитано
         * по своему каталогу (10.09.2026, 3 605 товаров с описанием
         * из 3 762 активных): средняя длина 1 671 знак, медиана 1 310,
         * максимум 20 695.
         *
         *   потолок 2000 — обрежется у 28,3% товаров
         *   потолок 3000 — у 11,7%
         *   потолок 4000 — у 5,9%
         *
         * Совпало с донорским почти в точности (у него на 3000 — 13%),
         * так что 3 000 остаются. Режем не ради экономии — ход с
         * инструментом стоит примерно одинаково с описанием и без него, —
         * а ради потолка на самый жирный случай: без него один вопрос
         * о товаре с описанием на двадцать тысяч знаков стоил бы дороже
         * всего дневного расхода на бота.
         */
        'product_description_limit' => max(200, (int) env('AI_PRODUCT_DESCRIPTION_LIMIT', 3000)),

        /*
         * Сколько строк характеристик уходит в модель у товара В СПИСКЕ выдачи.
         * У одной карточки (get_product) список полный — там спросили про
         * ОДИН товар, и обрезка теряла бы как раз то, за чем пришли.
         *
         * Число измерено по своему каталогу (дев, 15.09.2026): медиана — 14
         * строк, но хвост длинный, до 67. Пять карточек по 67 строк — это
         * выдача, в которой ответ утонет, а первые строки `specs` заполняет
         * импорт самым существенным (мощность, производительность, давление).
         */
        'product_specs_in_list' => max(1, (int) env('AI_PRODUCT_SPECS_IN_LIST', 8)),
    ],

    /*
    |--------------------------------------------------------------------------
    | Смысловой поиск по каталогу
    |--------------------------------------------------------------------------
    |
    | Векторы товаров живут в ОТДЕЛЬНОМ зеркале `products_semantic`, а не
    | в витринном `stankoman_products`. Это не вкусовщина: Meilisearch
    | с объявленным эмбеддером `userProvided` отвергает документ без вектора
    | целиком, а Scout шлёт документы пачками и про векторы не знает —
    | любой новый товар и вся пачка импорта выпадали бы из поиска ВИТРИНЫ
    | до ближайшей ночи. Витринный индекс эмбеддер не объявляет.
    |
    | Гибридный поиск даёт одним запросом и слова, и смысл, и фильтры
    | по цене, наличию и разделу. Для артикулов это важнее чистых векторов:
    | «ВК-J 15/10 TG» требует точного совпадения, «для гаража» — смысла.
    |
    */
    'catalog_search' => [
        /*
         * Зеркальный индекс и имя эмбеддера в его настройках. Лежат здесь,
         * а не строками в трёх местах: меняются они вместе.
         */
        'index' => (string) env('AI_CATALOG_INDEX', 'products_semantic'),
        'embedder' => (string) env('AI_CATALOG_EMBEDDER', 'shop'),

        /*
         * Потолок текста, уходящего в вектор. У донора 1200 знаков покрывали
         * средний разобранный текст товара целиком. У нас описания длиннее
         * (медиана 1 310 знаков против донорских 1 301 на весь текст), так
         * что обрезка коснётся половины каталога — но обрезается ХВОСТ,
         * а в начале собранного текста стоят название, бренд и
         * характеристики, то есть то, по чему и ищут. Пересмотреть после
         * первых живых замеров выдачи.
         */
        'text_limit' => max(200, (int) env('AI_CATALOG_TEXT_LIMIT', 1200)),

        /*
         * Доля смысла в гибриде: 0 — только слова, 1 — только вектор.
         */
        'semantic_ratio' => max(0.0, min(1.0, (float) env('AI_CATALOG_SEMANTIC_RATIO', 0.5))),

        /*
         * Доля смысла для запроса-обозначения: артикул, модель, НАЗВАНИЕ
         * БРЕНДА. Замер донора на «ВК-J 15/10»: до 0.3 точная модель держится
         * первой, на 0.5 хвост засоряется чужими товарами, на 0.8 модель
         * теряется совсем.
         *
         * Бренд попал в ту же корзину дорогой ценой (kratonshop, 09.09.2026):
         * на «Харсман есть?» слова находили Hansmann в обоих индексах, но
         * попадание с одной опечаткой стоит 0.494, а вектор выдуманного слова
         * давал случайному товару 0.783 — при доле 0.5 побеждал алмазный круг,
         * и бот отвечал, что бренда в каталоге нет. Дело не в пороге,
         * а в несравнимости шкал: 0.78 здесь не сходство, а шумовой пол,
         * «Квазимодо» получает 0.750 против того же товара.
         *
         * Ноль не ставим — небольшая примесь смысла подтягивает соседние
         * исполнения той же серии, а это как раз то, что покупателю полезно.
         */
        'semantic_ratio_model' => max(0.0, min(1.0, (float) env('AI_CATALOG_SEMANTIC_RATIO_MODEL', 0.2))),

        /*
         * Сколько товаров эмбеддить за один вызов шлюза. Вынесено отдельно
         * от общего размера партии: здесь тексты длиннее фрагментов базы.
         */
        'batch' => max(1, (int) env('AI_CATALOG_EMBED_BATCH', 32)),

        /*
         * Слова, которые CatalogBrands не должен принимать за бренд каталога.
         *
         * Замер 15.09.2026: донорские правила на нашем списке из 60 брендов
         * дали 15 ложных срабатываний на 134 обычных вопросах, и почти все —
         * на словах, без которых магазин станков не разговаривает. «Сталь»,
         * «стальной», «стали» уходили в Stalex; «металла», «толщина металла»
         * — в MetalMaster; «сверла» — в Everlast; «зерно» — в Zero. Причина
         * не в пороге, а в окончаниях: «стали» отстоит от начала «stalex»
         * ровно на одну замену, как «харсман» от «hansmann», и допуск честно
         * прощает оба. Правилом их не разделить — разделяет знание языка.
         *
         * Цена ошибки не косметическая: узнав «бренд», поиск снимает фильтр
         * по типу техники (см. CatalogBrands), то есть «станок для резки
         * металла» с типом «ленточнопильный» терял бы тип именно там, где он
         * нужнее всего.
         *
         * Список сравнивается по НАЧАЛУ слова и до транслитерации, поэтому
         * основы хватает на все падежи. Бренд, записанный кириллицей, от него
         * не страдает: так пишут «хансман» и «ремеза», но не «стали» вместо
         * Stalex. Остаются известные и мягкие двойники — «дали» против бренда
         * Dali и «старт» против START: их мы теряем сознательно, потому что
         * так покупатель пишет и сам бренд, а последствие — всего лишь
         * несработавший фильтр по типу. Новый поставщик с именем-словом
         * добавляется сюда после прогона tests/Unit/CatalogBrandsTest.php.
         */
        'brand_lookalikes' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('AI_CATALOG_BRAND_LOOKALIKES', 'стал,металл,сверл,зерн')),
        ))),
    ],

    /*
    |--------------------------------------------------------------------------
    | Эмбеддинги
    |--------------------------------------------------------------------------
    |
    | qwen3-embedding-8b: 1.90 ₽ за млн токенов против 13.68 у text-embedding-v4
    | — в семь раз дешевле при том же качестве разделения на пробах донора
    | (близкие тексты 0.663, далёкие 0.280).
    |
    | Родная размерность 4096 избыточна. Модель матрёшечная (MRL), параметр
    | dimensions работает — проверено на нашем ключе 10.09.2026: запрос
    | с dimensions=1024 вернул ровно 1024 числа. Берём 1024: вчетверо меньше
    | места и перебора, 4 КБ на фрагмент.
    |
    | Модель симметричная — один и тот же вызов для индексации и для запроса.
    | Смена модели ИЛИ размерности обязывает к полной переиндексации, поэтому
    | оба значения пишутся рядом с каждым вектором и сверяются при поиске.
    |
    */
    'embedding' => [
        'model' => env('AI_EMBEDDING_MODEL', 'qwen3-embedding-8b'),
        'dimensions' => max(64, (int) env('AI_EMBEDDING_DIMENSIONS', 1024)),
        'batch_size' => max(1, (int) env('AI_EMBEDDING_BATCH', 64)),

        /*
         * Кэш эмбеддинга вопроса в Redis по нормализованной формулировке:
         * у магазинного FAQ короткий хвост, до API доходят только новые
         * формулировки.
         */
        'query_cache_ttl' => max(0, (int) env('AI_EMBEDDING_QUERY_CACHE_TTL', 604800)),

        /*
         * Детерминированный вектор из хэша вместо вызова API. Качество поиска
         * при этом бессмысленно, но весь тракт чанк → вставка → поиск →
         * tool_result прогоняется без сети. Только для тестов.
         */
        'fake' => filter_var(env('AI_EMBEDDING_FAKE', false), FILTER_VALIDATE_BOOL),
    ],

    /*
    |--------------------------------------------------------------------------
    | База знаний
    |--------------------------------------------------------------------------
    |
    | Векторы лежат в MySQL, косинус считается в PHP. Корпус здесь меньше
    | донорского, так что запаса хватит надолго. Товарный слой сюда не
    | поместится — он живёт в зеркале Meilisearch (см. выше).
    |
    */
    'knowledge_base' => [
        'table' => 'kb_chunks',
        'default_top_k' => max(1, (int) env('AI_KB_TOP_K', 5)),

        /*
         * Порог релевантности. Замерен на СВОЕЙ базе `ai:kb-calibrate`
         * 15.09.2026: 31 вопрос (App\Shop\KbCalibrationQuestions), 10 фрагментов,
         * qwen3-embedding-8b / 1024:
         *
         *   посторонняя тема          0.272 … 0.408   медиана 0.319
         *   по теме, ответ есть       0.431 … 0.608   медиана 0.517
         *   по теме, ответа нет       0.360 … 0.522   медиана 0.440
         *
         * Донорские 0.33 здесь пропускали двух посторонних из восьми: «сколько
         * будет 2+2» и «курс доллара» находили фрагмент из одной строки «Ставка
         * НДС: 22%» (0.458 и 0.440 — выше самых слабых настоящих ответов, так что
         * порогом было не отсечь). Строку перенесли внутрь раздела «Организация»,
         * и группы разошлись: без ошибок пороги 0.41–0.43, берём середину.
         *
         * Зазор узкий — 0.023 между «2+2» (0.408) и «где вы находитесь» (0.431).
         * Ошибка вниз дешевле ошибки вверх: пропущенный ответ оборачивается
         * «уточню у менеджера», а не чужим контекстом для модели. После каждой
         * новой статьи базы знаний — поправить список вопросов и перемерить.
         *
         * Вывод донора подтвердился и здесь: порог НЕ отличает «ответ в базе
         * есть» от «по теме, но ответа нет» — 6 из 10 вопросов без ответа выше
         * 0.42. Значит kb_miss — сигнал СЛАБЫЙ, и трактовать его как «дыра
         * в базе» нельзя. Дыры находятся оценками 👎 и эскалациями.
         */
        'min_score' => (float) env('AI_KB_MIN_SCORE', 0.42),

        /*
         * Экран «Пробелы в базе знаний».
         *
         * Порог группировки — косинус между ВОПРОСАМИ, и он заметно выше
         * порога релевантности: там сравниваются вопрос и абзац страницы,
         * здесь — две человеческие формулировки одного и того же.
         *
         * Замер донора на 13 живых вопросах в четырёх темах: внутри темы
         * 0.487…0.815, между темами 0.424…0.695 — распределения
         * перекрываются, идеального порога нет, есть выбор породы ошибки.
         * Перебор на тех же векторах: 0.62 склеивает возврат со счетами
         * для юрлица, 0.66–0.68 восстанавливают все четыре темы ровно,
         * с 0.69 начинает рассыпаться «счёт на ООО». Берём 0.68 — верх
         * плато и максимальное расстояние до обрыва.
         *
         * Ошибка в сторону дробления безопаснее: рассыпавшуюся группу админ
         * видит и складывает сам, а склеенная притворяется одной задачей.
         */
        'gaps' => [
            'cluster_threshold' => (float) env('AI_KB_GAPS_THRESHOLD', 0.68),

            /*
             * Потолок разбираемых сигналов — ограничение не машины,
             * а читателя: очередь работ длиной в тысячу строк очередью
             * не является.
             */
            'max_signals' => max(20, (int) env('AI_KB_GAPS_LIMIT', 500)),
        ],

        /*
         * Черновик статьи ботом.
         *
         * Потолок ответа щедрее агентского: там 1024 токена на реплику
         * в чате, здесь — целая статья вместе со списком недостающих фактов.
         * Дороже это копейки: черновик собирают единицы раз в неделю.
         *
         * `top_k` намеренно больше поискового: бот отвечает покупателю
         * по лучшему фрагменту, а статью пишут, оглядываясь на всё, что уже
         * написано, — иначе она повторит соседнюю страницу своими словами.
         */
        'draft' => [
            'max_tokens' => max(256, (int) env('AI_KB_DRAFT_MAX_TOKENS', 1200)),
            'top_k' => max(1, (int) env('AI_KB_DRAFT_TOP_K', 6)),
            'ttl' => max(60, (int) env('AI_KB_DRAFT_TTL', 1800)),
        ],

        /*
         * Страницы сайта, попадающие в базу знаний. Список, а не «все
         * подряд»: в базе должно лежать то, что отвечает на вопросы,
         * а не витринные тексты.
         *
         * Чем это не косметика — замер донора. Страница «Отгрузки» (галерея
         * прошлых поставок) давала 11% корпуса, но всплывала в топ-3 у ШЕСТИ
         * вопросов из 24 и занимала первое место у трёх — включая «какая
         * погода в москве». Ответить она не может ни на один: близость
         * возникает по словам «компрессор», «станок», «поставка». Для бота
         * это худший вид шума — он не приводит к отказу, он даёт
         * правдоподобный на вид контекст, на котором строится неверный ответ.
         *
         * Список ниже собран по замеру своих страниц 10.09.2026 (16
         * опубликованных, из них шесть пустые — 7 байт, `<p></p>`):
         *
         *   dostavka-i-oplata  1 269 знаков текста — доставка, оплата, сроки
         *   o-kompanii         2 004 — кто мы, чем занимаемся
         *   kontakty             614 — адрес, реквизиты, телефон, почта
         *   production           701 — сварка ленточных пил на ЧПУ, полотна
         *                              под размер: это услуга, а не витрина,
         *                              и на вопрос «сварите под мой станок»
         *                              отвечает по существу
         *   service              503 — что делает поддержка: подбор запчастей
         *                              и аналогов, инструкции, гарантия
         *
         * Кого сюда НЕ берём и почему:
         *
         *   sertifikaty-i-licenzii — 122 знака текста: одна фраза «является
         *       дилером более чем ста производителей» и дальше картинки.
         *       Это ровно донорский случай: фрагмент выиграет выдачу на
         *       «сертификат» и «официальный дилер», не ответив ничего.
         *       Появится текст — вернём;
         *   terms, privacy — юридические простыни на 9,5 тысяч знаков каждая,
         *       вдвое длиннее всего полезного корпуса. Они выиграют выдачу
         *       на «возврат», «гарантия», «оплата», и бот зачитает
         *       нецитируемый юридический текст вслух;
         *   home, novosti, karera — витрина, пустышки и вакансии: покупателя
         *       не касаются;
         *   arenda-oborudovaniia, dokumenty, remont-texniki, servisnyi-centr,
         *   sotrudniki — пустые страницы.
         *
         * Итого ~5 100 знаков ≈ 6–8 фрагментов. Донор считал тонким корпус
         * из 27. Это и есть главный риск проекта: базу знаний надо ПИСАТЬ,
         * а не собирать.
         */
        'static_pages' => [
            'dostavka-i-oplata',
            'o-kompanii',
            'kontakty',
            'production',
            'service',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Виджет и диалоги
    |--------------------------------------------------------------------------
    |
    | Посетитель анонимен, и опознаёт его единственный секрет — токен диалога
    | в вечной httpOnly-куке, которую ставит СЕРВЕР. Не localStorage: в
    | переписке может оказаться телефон, а токен даёт к ней доступ — из
    | localStorage его унесёт любой XSS. Плюс Safari режет срок жизни кук,
    | выставленных из JS, до семи дней, а серверные httpOnly не режет.
    |
    | Лесенка поллинга и потолок ожидания выведены из замера донора, а не из
    | общих соображений: p50 5.6 с, p95 34 с, максимум 58 с, и разброс —
    | на стороне шлюза, убрать его нельзя. Отсюда «секунду, уточняю…»
    | на двадцатой секунде и отказ останавливать опрос раньше минуты.
    |
    */
    'chat' => [
        'cookie' => env('AI_CHAT_COOKIE', 'intertooler_chat'),

        /*
         * Хосты магазина: с них ссылка в ленте остаётся кликабельной,
         * как со своей витрины.
         *
         * Список ЗАКРЫТЫЙ и правится только разработчиком: в нём вся защита
         * от подписи-обманки «[Оплатить заказ](http://evil.ru)». Нужен он
         * сверх APP_URL потому, что на деве APP_URL — это localhost:8103,
         * и проверять поведение ссылок на стенде, где оно другое, значит
         * проверять не то.
         */
        'own_hosts' => array_values(array_filter(array_map(
            'trim',
            explode(',', (string) env('AI_CHAT_OWN_HOSTS', 'intertooler.ru,www.intertooler.ru')),
        ))),

        // Столько сообщений переписки уходит в модель как история.
        // Двадцать — это десять ходов: на магазинном вопросе больше
        // не нужно, а каждое лишнее сообщение оплачивается на каждом ходе.
        'history_messages' => max(2, (int) env('AI_CHAT_HISTORY', 20)),

        'max_message_length' => max(64, (int) env('AI_CHAT_MAX_LENGTH', 1000)),

        /*
         * Жизнь пометки «ответ готовится» в кэше. Заведомо больше таймаута
         * джобы (200 с): пометка обязана пережить самый долгий ответ, иначе
         * посетитель увидит «печатает», которое кончилось ничем.
         */
        'pending_ttl' => max(60, (int) env('AI_CHAT_PENDING_TTL', 210)),

        /*
         * Жизнь пометки «оператор печатает». Должна пережить и паузу между
         * его нажатиями, и тик опроса у посетителя, иначе «печатает» мигало
         * бы на ровном месте. Но не сильно больше: это обещание ответа.
         */
        'typing_ttl' => max(5, (int) env('AI_CHAT_TYPING_TTL', 12)),

        /*
         * Признаки того, что очередь ответов не разгребается. Оба нужны
         * вместе: по размеру не поймать мёртвый воркер на первой задаче,
         * по отметке живости — не отличить аварию от тихой ночи.
         */
        'queue_backlog_limit' => max(1, (int) env('AI_CHAT_QUEUE_BACKLOG', 3)),
        'queue_silence' => max(60, (int) env('AI_CHAT_QUEUE_SILENCE', 300)),

        /*
         * Намордник. Все значения читает один класс — ChatAbuseGuard,
         * и проверяются они тестом без базы и без сети.
         *
         * Слоёв два, и прикладной — второй. Первый стоит в nginx: пока
         * запрос не дошёл до PHP, никакие потолки ниже не сработают, потому
         * что воркеры FPM уже заняты. Здесь — то, что умеет только
         * приложение: кто спрашивает, о чём и за чьи деньги.
         *
         * Каждое число отвечает на свой вопрос, подменять одно другим нельзя:
         *
         *   cooldown          — быстрее человека никто не печатает;
         *   на диалог в час   — один посетитель за один присест;
         *   на диалог в сутки — он же, но за всю смену;
         *   на адрес в сутки  — офис за одним NAT, где диалогов много;
         *   новых диалогов    — тот, кто чистит куку, чтобы начать заново.
         *
         * Потолок на диалог намеренно СУТОЧНЫЙ, а не пожизненный. Кука
         * вечная, разговор живёт годами, и пожизненные тридцать сообщений
         * однажды заперли бы постоянного покупателя в мёртвом чате.
         */
        'abuse' => [
            // Нижняя граница ловит не абуз, а случайный Enter: «а», «??».
            'min_length' => max(1, (int) env('AI_CHAT_MIN_LENGTH', 2)),

            'cooldown_seconds' => max(0, (int) env('AI_CHAT_COOLDOWN', 3)),

            'per_conversation_hour' => max(1, (int) env('AI_CHAT_RATE_CONVERSATION_HOUR', 15)),
            'per_conversation_day' => max(1, (int) env('AI_CHAT_RATE_CONVERSATION_DAY', 30)),

            /*
             * Потолки на АДРЕС у донора поднимали после приёмки, и подняли
             * справедливо: магазин продаёт организациям, а офис сидит за
             * одним NAT. Сорок сообщений в сутки на адрес — это два сообщения
             * на человека в конторе из двадцати, а три новых разговора —
             * это четвёртый сотрудник, которому чат отвечает «слишком много
             * обращений». У нас та же публика: в заказах есть ИНН и КПП.
             *
             * Второй путь в тот же тупик — «Очистить переписку»: каждая
             * чистка начинает новый разговор. Счётчик по адресу чистку
             * переживает намеренно (иначе кнопка стала бы способом обнулить
             * лимиты), поэтому места для манёвра у него быть не должно.
             */
            'per_ip_day' => max(1, (int) env('AI_CHAT_RATE_IP_DAY', 150)),
            'new_conversations_per_ip_day' => max(1, (int) env('AI_CHAT_RATE_NEW_CONVERSATIONS', 10)),

            /*
             * Дневной бюджет — единственный потолок, который считается не
             * по посетителю, а по магазину, и исход у него другой: не отказ,
             * а звонок. Бот замолкает, чат вырождается в форму обратного
             * звонка, вопрос уходит менеджеру штатной эскалацией.
             *
             * Ход стоит 0.02–0.06 ₽ при кэше префикса. Пятьсот вопросов
             * в сутки — это порядка 30 ₽, и упереться в них можно только
             * ботом или ошибкой в нашем же коде.
             *
             * Ноль отключает потолок. Не путать с бюджетом на ключе
             * aitunnel: тот режет на стороне шлюза и жёстко, этот — наш
             * собственный и мягкий. И он же единственная защита кошелька
             * turbo, пока ключ общий.
             */
            'daily_messages' => max(0, (int) env('AI_CHAT_DAILY_MESSAGES', 500)),
            'daily_tokens' => max(0, (int) env('AI_CHAT_DAILY_TOKENS', 2000000)),

            /*
             * Капча — только на ПЕРВОМ сообщении диалога. Дальше её не
             * спрашиваем: посетителя удостоверяет кука разговора, а лишний
             * вызов сервиса на каждую реплику — это и задержка, и запрос
             * к третьей стороне на каждый вопрос покупателя.
             *
             * Включена она или нет — здесь не решается и не дублируется:
             * это config/captcha.php, один рубильник на весь магазин.
             */
            'captcha_first_message' => filter_var(env('AI_CHAT_CAPTCHA_FIRST', true), FILTER_VALIDATE_BOOL),
        ],

        /*
         * Кэш ответов на повторные вопросы.
         *
         * Неделя выбрана не из осторожности, а из устройства данных:
         * кэшируются только ответы по базе знаний, а она меняется правкой
         * страницы или статьи — то есть раз в недели, не в часы. Всё живое
         * (цена, наличие) в кэш не попадает вовсе.
         *
         * Ноль отключает кэш целиком.
         */
        'answer_cache_ttl' => max(0, (int) env('AI_CHAT_ANSWER_CACHE_TTL', 604800)),

        /*
         * Предпросмотр: виджет выкачен, но виден не всем.
         *
         * Нужен, чтобы код приехал на настоящий сайт раньше, чем виджет
         * увидят покупатели: заказчик смотрит на живом каталоге, а не на
         * копии, где товары вчерашние. Он же канареечный выкат.
         *
         * Сотрудники магазина видят виджет всегда. Остальные — по ссылке
         * `?bot=<ключ>`, которая запоминается кукой на год.
         *
         * Пустой ключ при включённом предпросмотре означает «никому»,
         * а не «всем»: забытая настройка не должна открывать виджет молча.
         */
        'preview' => [
            'enabled' => filter_var(env('AI_CHAT_PREVIEW', false), FILTER_VALIDATE_BOOL),
            'key' => (string) env('AI_CHAT_PREVIEW_KEY', ''),
        ],

        /*
         * Сроки хранения переписки (команда `chat:purge`).
         *
         * Две ступени, а не одна, потому что ценность у данных разная.
         * Хэш адреса, user-agent и referer нужны для разбора абуза — то есть
         * дни, а не месяцы; переписка нужна магазину как след разговора
         * с клиентом — то есть месяцы. Поэтому сначала диалог обезличивается,
         * и только потом, много позже, удаляется целиком.
         *
         * Тридцать и сто восемьдесят дней — не требование закона (152-ФЗ
         * сроков не называет, он требует не хранить дольше, чем нужно для
         * цели), а наш собственный ответ на вопрос «зачем нам это через
         * полгода». Ответа не нашлось, поэтому удаляем.
         *
         * Ноль отключает соответствующую ступень.
         */
        'retention' => [
            'anonymize_days' => max(0, (int) env('AI_CHAT_ANONYMIZE_DAYS', 30)),
            'purge_days' => max(0, (int) env('AI_CHAT_PURGE_DAYS', 180)),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Присутствие оператора
    |--------------------------------------------------------------------------
    |
    | «Есть ли сейчас живой человек» — не статичное расписание, а сигнал из
    | трёх источников, и приоритет между ними важнее самих значений:
    |
    |   1) ручной переключатель в топбаре админки — перебивает всё, но живёт
    |      не дольше override_ttl. Без срока жизни он опаснее пользы: включил
    |      «я на смене», ушёл в отпуск — и магазин месяцами обещает живого
    |      оператора в четыре утра;
    |   2) активность в админке — она и так опрашивает уведомления, значит
    |      открытая вкладка это и есть «человек за столом»;
    |   3) расписание — база, когда первых двух сигналов нет.
    |
    | От сигнала зависят три вещи: показывать в виджете «Позвать оператора»
    | или «Оставить телефон», как бот формулирует эскалацию и уходит ли пуш
    | в MAX сразу или ждёт начала смены.
    |
    */
    'operators' => [
        /*
         * Часы работы по дням недели, ключ — числом ISO-8601 (1 = понедельник),
         * null означает выходной.
         *
         * Это только значение ПО УМОЛЧАНИЮ. Режим работы у магазина один на
         * сайт и чат — настройка `company.work_schedule` («Настройки» → «Режим
         * работы», форма та же: день → [с, до] или null). Из неё берут шапка,
         * подвал, блок на страницах, база знаний бота и статус «менеджер на
         * связи»; сюда код заглядывает, лишь когда её нет.
         *
         * Своего расписания у ассистента нет намеренно, в отличие от kratonshop
         * (`chat.schedule` на странице настроек ассистента): два места для одного
         * факта здесь уже разошлись — «О компании» 9–17 против 9–18 в шапке.
         */
        'schedule' => [
            1 => ['09:00', '18:00'],
            2 => ['09:00', '18:00'],
            3 => ['09:00', '18:00'],
            4 => ['09:00', '18:00'],
            5 => ['09:00', '18:00'],
            6 => null,
            7 => null,
        ],

        'timezone' => env('AI_OPERATORS_TIMEZONE', 'Europe/Moscow'),

        /*
         * Сколько минут после последнего действия в админке человек ещё
         * считается на месте. Десять — компромисс: меньше гасит присутствие
         * на время телефонного разговора, больше обещает оператора тому,
         * кто уже ушёл.
         */
        'activity_window_minutes' => max(1, (int) env('AI_OPERATORS_ACTIVITY_WINDOW', 10)),

        /*
         * Срок жизни ручного переключателя. Смена длиннее восьми часов —
         * повод нажать кнопку ещё раз, а не повод забыть о ней навсегда.
         */
        'override_ttl_minutes' => max(5, (int) env('AI_OPERATORS_OVERRIDE_TTL', 480)),
    ],

    /*
    |--------------------------------------------------------------------------
    | Эскалация
    |--------------------------------------------------------------------------
    |
    | Источник истины по эскалации — уведомление в самой админке: оно не
    | зависит ни от почтового релея, ни от чужого API. Почта и MAX — доставка
    | того же события до человека, который в админку сейчас не смотрит,
    | и обе best-effort: их сбой пишется в лог и не роняет джобу.
    |
    */
    'escalation' => [
        'notify_database' => filter_var(env('AI_ESCALATION_NOTIFY_DB', true), FILTER_VALIDATE_BOOL),
        'notify_mail' => filter_var(env('AI_ESCALATION_NOTIFY_MAIL', true), FILTER_VALIDATE_BOOL),

        // Канал самоотключается без токена, поэтому флаг ничего не стоит
        // держать включённым до решения, нужен ли MAX этому магазину.
        'notify_max' => filter_var(env('AI_ESCALATION_NOTIFY_MAX', true), FILTER_VALIDATE_BOOL),

        /*
         * Пуш в MAX ночью бессмыслен: он разбудит и ничего не изменит —
         * ответить всё равно некому. Поэтому вне смены пуш откладывается
         * до её начала, а письмо и уведомление в админке уходят сразу.
         */
        'defer_max_until_shift' => filter_var(env('AI_ESCALATION_DEFER_MAX', true), FILTER_VALIDATE_BOOL),

        /*
         * Не чаще одного письма об этом диалоге за час. Посетитель может
         * задать подряд три вопроса, на которые бот не ответит, — менеджеру
         * нужен один сигнал, а не три.
         */
        'notify_cooldown_minutes' => max(0, (int) env('AI_ESCALATION_COOLDOWN', 60)),
    ],

];
